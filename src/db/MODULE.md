# db Module
<!-- GENERATED: Do not edit manually. Regenerated by task-closer/cleanup. -->

> Layer: L1 (Data Persistence) | 6 files | 919 lines
> Rules: docs/generated/architecture-rules.json#L1

## Dependencies (import from)
- core (L0): logger, types

## Dependents (imported by)
- config (L1), candles (L3), vectors (L3), filters (L4), knn (L4), signals (L5), positions (L5), limits (L5), orders (L6), exits (L6), labeling (L6), notifications (L7), daemon (L9), backtest (L8)

## Public Interface
- `initDb(url)` → DbInstance
- `getDb()` / `getPool()` / `closePool()` / `isHealthy()`
- `insertEvent(params)` / `queryEvents(filters)`
- `makeGetActiveTickets()` — FOR UPDATE locking
- Tables: symbolTable, candleTable, ticketTable, signalTable, vectorTable, orderTable, +7

## Files
| File | Purpose | Exports | Lines |
|------|---------|---------|-------|
| schema.ts | Drizzle table definitions (13 tables) | `symbolTable`, `candleTable`, `ticketTable`, `signalTable`, `vectorTable`, `orderTable`, +types | 575 |
| event-log.ts | Event logging insert/query | `insertEvent`, `queryEvents`, `InsertEventParams` | 132 |
| pool.ts | PostgreSQL connection pool | `initDb`, `getDb`, `getPool`, `closePool`, `DbInstance` | 124 |
| queries.ts | Shared query builders | `makeGetActiveTickets` | 54 |
| migrate.ts | Migration runner | (side-effect) | 30 |
| index.ts | Barrel re-exports | — | 4 |
