# config Module
<!-- GENERATED: Do not edit manually. Regenerated by task-closer/cleanup. -->

> Layer: L1 (Data Persistence) | 4 files | 911 lines
> Rules: docs/generated/architecture-rules.json#L1

## Dependencies (import from)
- core (L0): constants, logger, types
- db (L1): pool, schema

## Dependents (imported by)
- candles (L3)

## Public Interface
- `loadConfig(db)` → void (populates cache)
- `getConfig<T>(group, key)` → T
- `getGroupConfig(group)` → Record
- `refreshConfig()` / `updateConfig(group, key, value)` / `watchConfig(cb)`
- `seed(db)` — populates CommonCode defaults
- Zod schemas: ExchangeConfigSchema, KnnConfigSchema, PositionConfigSchema, +10

## Files
| File | Purpose | Exports | Lines |
|------|---------|---------|-------|
| seed.ts | Default CommonCode seed data | `SEED_DATA`, `seed` | 429 |
| schema.ts | Zod validation schemas | `ExchangeConfigSchema`, `KnnConfigSchema`, `CONFIG_SCHEMAS`, +10 | 223 |
| index.ts | Config API facade | `loadConfig`, `getConfig`, `refreshConfig`, `updateConfig`, `watchConfig` | 133 |
| loader.ts | In-memory config cache | `loadAllConfig`, `getCachedValue`, `clearCache` | 126 |
