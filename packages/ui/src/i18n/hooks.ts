/**
 * i18n hooks — re-exports from next-intl.
 *
 * Provides a single import point for translation hooks so that
 * components in packages/ui don't need to import next-intl directly.
 */

export { useTranslations, useLocale, useFormatter } from "next-intl";
