"use client";

export default function DashboardPage() {
	return (
		<main
			style={{
				display: "flex",
				alignItems: "center",
				justifyContent: "center",
				height: "100vh",
				color: "var(--text-primary)",
				fontFamily: "var(--font-sans)",
			}}
		>
			Combine Trade Desktop
		</main>
	);
}
