# signals Module
<!-- GENERATED: Do not edit manually. Regenerated by task-closer/cleanup. -->

> Layer: L5 (Decision) | 4 files | 1,083 lines
> Rules: docs/generated/architecture-rules.json#L5

## Dependencies (import from)
- core (L0): types, decimal
- db (L1): pool, schema
- indicators (L2): types (AllIndicators)

## Dependents (imported by)
- daemon (L9), backtest (L8)

## Public Interface
- `detectWatching(indicators, candles)` → WatchingResult
- `openWatchSession(db, params)` / `invalidateWatchSession(db, id)`
- `checkEvidence(session, indicators)` → EvidenceResult
- `createSignal(db, params)` / `calcSlPrice(entry, direction, atr)`
- `checkSafety(indicators, signal)` → SafetyResult

## Files
| File | Purpose | Exports | Lines |
|------|---------|---------|-------|
| watching.ts | Watch session lifecycle | `detectWatching`, `checkInvalidation`, `openWatchSession` | 470 |
| safety-gate.ts | Safety filter (wick, box, abnormal) | `checkSafety`, `WICK_RATIO_THRESHOLD`, `BOX_MA20_MARGIN_RATIO` | 320 |
| evidence-gate.ts | Entry evidence validation | `checkEvidence`, `createSignal`, `calcSlPrice` | 281 |
| index.ts | Barrel re-exports | — | 12 |
