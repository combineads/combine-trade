# limits Module
<!-- GENERATED: Do not edit manually. Regenerated by task-closer/cleanup. -->

> Layer: L5 (Decision) | 2 files | 443 lines
> Rules: docs/generated/architecture-rules.json#L5

## Dependencies (import from)
- core (L0): decimal
- db (L1): pool, schema

## Dependents (imported by)
- daemon (L9), backtest (L8)

## Public Interface
- `checkLossLimit(db, symbol)` → LossLimitResult
- `checkAccountDailyLimit(db)` → AccountDailyLimitResult
- `recordLoss(db, symbol, amount)`
- `resetDailyLosses()` / `resetSessionLosses()` / `resetHourlyLosses()`
- `loadLossLimitConfig(db)` → LossLimitConfig

## Files
| File | Purpose | Exports | Lines |
|------|---------|---------|-------|
| loss-limit.ts | Daily/session/hourly loss checks | `checkLossLimit`, `recordLoss`, `resetAllExpired`, +12 | 424 |
| index.ts | Barrel re-exports | — | 19 |
