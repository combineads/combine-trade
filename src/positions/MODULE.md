# positions Module
<!-- GENERATED: Do not edit manually. Regenerated by task-closer/cleanup. -->

> Layer: L5 (Decision) | 5 files | 1,184 lines
> Rules: docs/generated/architecture-rules.json#L5

## Dependencies (import from)
- core (L0): types, decimal, ports, logger
- db (L1): pool, schema

## Dependents (imported by)
- daemon (L9), backtest (L8)

## Public Interface
- `createTicket(db, params)` / `transitionTicket(db, id, event)` / `closeTicket(db, params)`
- `getActiveTicket(db, symbol)` / `getTicketById(db, id)`
- `canPyramid(db, ticket, config)` → PyramidCheckResult
- `executePyramid(params)` → EntryResult
- `calculateSize(params)` → SizeResult
- FSM: `canTransition()`, `validateTransition()`, `getNextState()`

## Files
| File | Purpose | Exports | Lines |
|------|---------|---------|-------|
| ticket-manager.ts | Ticket CRUD + state transitions | `createTicket`, `transitionTicket`, `closeTicket` | 386 |
| pyramid.ts | Pyramid entry logic | `canPyramid`, `executePyramid`, `loadPyramidConfig` | 353 |
| sizer.ts | Position sizing from config | `calculateSize`, `getRiskPct`, `SizeResult` | 225 |
| fsm.ts | State machine (IDLE→WATCHING→HAS_POSITION) | `canTransition`, `validateTransition`, `getNextState` | 181 |
| index.ts | Barrel re-exports | — | 39 |
